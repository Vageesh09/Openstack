import logging
import datetime
LOG_FILENAME = 'example.log'
logging.basicConfig(filename=LOG_FILENAME,level=logging.DEBUG)
print datetime.datetime.now()
logging.debug( str(datetime.datetime.now()) + ' vm created')
logging.error( str(datetime.datetime.now()) + ' vm created')
